# InDesign Translation Library (written in Rust)

This is a command line tool / library for translating InDesign files.

# Run locally

Run locally using ```cargo run [COMMAND] [FILENAME]```
For example
```cargo run extract test.idml```